"""Local browser UI for the converter.

A tiny stdlib-only HTTP server (no third-party deps) that serves a drag-and-drop
page and runs real ffmpeg conversions through the reusable ``converter`` core.
Run it with ``python serve.py``.
"""
